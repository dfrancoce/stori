# Stori_server 

Node.js + Socket.io server


# Requirements

The following dependencies need to be installed:

* `npm install express`
* `npm install socket.io`
* `npm install mongojs`
* `npm install mocha`
